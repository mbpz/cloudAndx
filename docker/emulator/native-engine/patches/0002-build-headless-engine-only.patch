diff --git a/CMakeLists.txt b/CMakeLists.txt
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -116,6 +116,11 @@ set(OPTION_MINBUILD
       BOOL
       "Minimum set of components to support 64-bit guets only."
 )
+set(OPTION_HEADLESS_ENGINE_ONLY
+    FALSE
+    CACHE BOOL
+          "Build only the x86_64 headless engine, without the Qt launcher/UI targets."
+)
 set(OPTION_X86_64_GUEST_ON_AARCH64
     FALSE
     CACHE
@@ -1018,14 +1023,19 @@
   # Create the qemu targets.
-  if(NOT BUILDING_FOR_AARCH64 OR OPTION_X86_64_GUEST_ON_AARCH64)
+  if(OPTION_HEADLESS_ENGINE_ONLY)
+    android_add_qemu_headless_executable(
+      x86_64 "${ANDROID_QEMU_i386_DEVICES_${ANDROID_TARGET_TAG}}")
+  else()
+    if(NOT BUILDING_FOR_AARCH64 OR OPTION_X86_64_GUEST_ON_AARCH64)
+      android_add_qemu_executable(
+        x86_64 "${ANDROID_QEMU_i386_DEVICES_${ANDROID_TARGET_TAG}}")
+      android_add_qemu_headless_executable(
+        x86_64 "${ANDROID_QEMU_i386_DEVICES_${ANDROID_TARGET_TAG}}")
+    endif()
     android_add_qemu_executable(
-      x86_64 "${ANDROID_QEMU_i386_DEVICES_${ANDROID_TARGET_TAG}}")
+      aarch64 "${ANDROID_QEMU_arm_DEVICES_${ANDROID_TARGET_TAG}}")
     android_add_qemu_headless_executable(
-      x86_64 "${ANDROID_QEMU_i386_DEVICES_${ANDROID_TARGET_TAG}}")
-  endif()
-  android_add_qemu_executable(
-    aarch64 "${ANDROID_QEMU_arm_DEVICES_${ANDROID_TARGET_TAG}}")
-  android_add_qemu_headless_executable(
-    aarch64 "${ANDROID_QEMU_arm_DEVICES_${ANDROID_TARGET_TAG}}")
+      aarch64 "${ANDROID_QEMU_arm_DEVICES_${ANDROID_TARGET_TAG}}")
+  endif()
 
   if(NOT OPTION_MINBUILD)
     if(NOT BUILDING_FOR_AARCH64)
diff --git a/android/CMakeLists.txt b/android/CMakeLists.txt
--- a/android/CMakeLists.txt
+++ b/android/CMakeLists.txt
@@ -11,7 +11,9 @@ add_subdirectory(android-emugl)
 add_subdirectory(coverage-poc)
 add_subdirectory(android-ui)
 
-if(NOT OPTION_AEMU_LIBS_ONLY)
+if(OPTION_HEADLESS_ENGINE_ONLY)
+  # The QEMU headless target is declared by the top-level build.
+elseif(NOT OPTION_AEMU_LIBS_ONLY)
   add_subdirectory(emulator)
   add_subdirectory(emulator-check)
   if(NOT LINUX_AARCH64)
diff --git a/android/android-ui/CMakeLists.txt b/android/android-ui/CMakeLists.txt
--- a/android/android-ui/CMakeLists.txt
+++ b/android/android-ui/CMakeLists.txt
@@ -1,3 +1,5 @@
 # The list of dependencies.
-prebuilt(QT5)
-message(STATUS "Building UI using Qt ${QT_VERSION_MAJOR}.${QT_VERSION_MINOR}")
+if(NOT OPTION_HEADLESS_ENGINE_ONLY)
+  prebuilt(QT5)
+  message(STATUS "Building UI using Qt ${QT_VERSION_MAJOR}.${QT_VERSION_MINOR}")
+endif()
@@ -58,13 +60,16 @@ endfunction()
 
-add_subdirectory(apps)
 add_subdirectory(modules/aemu-gl-bridge)
 add_subdirectory(modules/aemu-gl-init)
 add_subdirectory(modules/aemu-ui-headless)
 add_subdirectory(modules/aemu-recording)
 add_subdirectory(modules/aemu-ui-common)
-add_subdirectory(modules/aemu-ui-widgets)
 add_subdirectory(modules/aemu-ui-window)
-add_subdirectory(modules/aemu-ui-qt)
-add_subdirectory(modules/aemu-ext-pages)
+
+if(NOT OPTION_HEADLESS_ENGINE_ONLY)
+  add_subdirectory(apps)
+  add_subdirectory(modules/aemu-ui-widgets)
+  add_subdirectory(modules/aemu-ui-qt)
+  add_subdirectory(modules/aemu-ext-pages)
+endif()
 
 list(APPEND android-libui-testdata testdata/mp4/video.mp4)
diff --git a/android/android-ui/modules/aemu-gl-bridge/CMakeLists.txt b/android/android-ui/modules/aemu-gl-bridge/CMakeLists.txt
--- a/android/android-ui/modules/aemu-gl-bridge/CMakeLists.txt
+++ b/android/android-ui/modules/aemu-gl-bridge/CMakeLists.txt
@@ -10,5 +10,6 @@ target_include_directories(aemu-gl-bridge PRIVATE .)
 target_compile_options(aemu-gl-bridge PRIVATE -Wno-return-type-c-linkage)
 android_target_link_libraries(aemu-gl-bridge linux-x86_64 PRIVATE -lX11)
 
+if(NOT OPTION_HEADLESS_ENGINE_ONLY)
 android_qt_lib(
   TARGET aemu-gl-qt-bridge
@@ -47,4 +48,5 @@ if(ENABLE_QT_TESTS)
     set_target_properties(aemu-gl-bridge-tests PROPERTIES INSTALL_RPATH "\$ORIGIN/lib64/qt/lib;\$ORIGIN/lib64")
   endif()
   set_target_properties(aemu-gl-bridge-tests PROPERTIES BUILD_WITH_INSTALL_RPATH TRUE)
 endif()
+endif()
diff --git a/android/emu/crashreport/CMakeLists.txt b/android/emu/crashreport/CMakeLists.txt
--- a/android/emu/crashreport/CMakeLists.txt
+++ b/android/emu/crashreport/CMakeLists.txt
@@ -1,2 +1,4 @@
-prebuilt(QT5)
+if(NOT OPTION_HEADLESS_ENGINE_ONLY)
+  prebuilt(QT5)
+endif()
 android_add_library(
@@ -53,5 +55,6 @@ target_compile_options(android-emu-crashreport PRIVATE -Wno-extern-c-compat
                        PUBLIC -Wno-macro-redefined)
 
 # This file defines emulator crash service
+if(NOT OPTION_HEADLESS_ENGINE_ONLY)
 android_add_library(
   TARGET android-emu-crashreport-consent-ui
@@ -81,4 +84,5 @@ target_include_directories(android-emu-crashreport-consent-ui PRIVATE src
 target_compile_options(android-emu-crashreport-consent-ui
                        PRIVATE -Wno-extern-c-compat)
+endif()
 
 android_add_library(
